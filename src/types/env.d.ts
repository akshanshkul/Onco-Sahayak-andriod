// Expo's CLI regenerates (and in SDK 57 removes) the root expo-env.d.ts, so the
// reference lives here where it is not managed. It supplies the ambient module
// declarations for asset and CSS imports, e.g. `import "./global.css"`.
/// <reference types="expo/types" />
/// <reference types="nativewind/types" />
